# System Architecture (Planned)

Smart Bin (Sensors + Camera)
→ Cloud API (Firebase Cloud Functions)
→ Firestore Database
→ Mobile App (Flutter)
→ Future Web Portal (Flutter Web)

Current phase: Mobile app + backend planning (no hardware integration yet).
